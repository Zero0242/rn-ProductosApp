import { StyleSheet } from "react-native";

export const loginStyles = StyleSheet.create({
    title: {
        color: 'white',
        fontWeight: 'bold',
        fontSize: 30,
        marginTop:20
    },
    label:{
        marginTop:25,
        color: 'white',
        fontWeight: 'bold',
    }
})